# HAgent.WinForms

Development host application for HAgent. This project is an executable and launches the HAgent configuration window directly. In Visual Studio, set **HAgent.WinForms** as the Startup Project for F5 debugging.
